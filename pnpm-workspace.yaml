# Explicit build-script decisions for pnpm v11. None of these need to build:
# esbuild ships its prebuilt binary via the @esbuild/<platform> package.
allowBuilds:
  core-js: false
  esbuild: false
  sharp: false
  unrs-resolver: false
  workerd: false
